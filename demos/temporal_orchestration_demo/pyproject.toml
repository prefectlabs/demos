[project]
name = "temporal-orchestration-demo"
version = "0.1.0"
description = "Temporal orchestration patterns with Prefect"
requires-python = ">=3.11"
dependencies = [
    "prefect>=3.4.0",
    "pydantic>=2.0.0",
]
